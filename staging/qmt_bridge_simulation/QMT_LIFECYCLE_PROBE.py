#coding:gbk
# Simulation-only QMT lifecycle probe. Keep ASCII-only in the QMT editor.

def init(ContextInfo):
    print("[qmt_probe] init ok")


def handlebar(ContextInfo):
    print("[qmt_probe] handlebar ok")
