# kitling_bigqmt 项目灵魂与工程宪章

## 1. 项目使命

本项目建设一套面向国金大 QMT 的可验证、可恢复、可审计交易基础平台，作为 MiniQMT 退出后的候选替代方案，并为 `kitling_AI量化_review` 后续的 ETF 与 A 股策略提供统一的行情、账户、执行、核算和数据出口。

项目首先服务于验证，而不是抢先上线。任何可运行、可连接或回测表现良好的结果，都不能自动获得正式盘下单资格。

## 2. 当前目标与边界

当前目标终端与模式：

- 模拟终端：`C:\BigQMT\work\国金QMT交易端模拟\bin.x64`。
- 正式终端：`C:\BigQMT\work\国金证券QMT交易端\bin.x64`。
- 模拟终端用于完整的查询、行情、下单、撤单、成交、恢复和策略验证。
- 正式终端在模拟盘验收前只允许只读验证。
- 首期资产：ETF 与 A 股现货。
- 首期策略：S10-D1 低频动量轮动 v1.1.15。
- 首期策略资金：100,000 元独立资金袖套，收益复利。
- QMT 内置运行环境按 Python 3.6 兼容约束开发。
- 实时行情与主机通信使用 Redis。
- 大型运行数据保存在 F 盘，不把长期行情、数据库和大型日志放入 E 盘 QMT 目录。
- 数据湖接入后置；先验证大 QMT 基础链路。
- 自动启动和自动登录属于最后阶段，不得先于核心链路和恢复机制实施。

费用、滑点、成交限制、接口字段和刷新延迟必须通过目标终端证据确认。未确认项标记为 `UNVERIFIED`，不得凭经验假定。

## 3. 最高安全门禁

### 3.1 模拟盘优先

在项目状态被正式标记为 `SIMULATION_VERIFIED` 之前：

- 正式盘允许查询行情、账户、持仓、委托和成交。
- 正式盘只读查询必须使用独立配置、独立账号标识、独立 Redis 命名空间和只读能力白名单。
- 禁止向正式盘发送任何买入、卖出、撤单、申购、赎回或其他可能改变账户状态的请求。
- 禁止在正式盘配置中启用任何订单方法。
- 禁止通过兼容层、调试脚本、RPC 通用调用、Dashboard、临时脚本或人工测试绕过只读限制。
- 禁止把模拟环境的 `orders_enabled=true` 配置复制到正式环境。

### 3.2 正式盘下单需要双重放行

即使模拟盘已经通过验收，正式盘下单仍不自动开放。必须同时满足：

1. 模拟盘门禁全部通过并有证据报告；
2. 项目状态明确为 `SIMULATION_VERIFIED`；
3. 正式盘只读验证通过；
4. 正式环境配置、账户、端口和 Redis 完全隔离；
5. 已完成回滚和紧急停机演练；
6. 用户对“启用正式盘下单”作出单独、明确的批准。

缺少其中任一项，正式盘必须保持 `READ_ONLY` 和 `orders_enabled=false`。

### 3.3 默认失败关闭

以下任一情况发生时禁止新增买入和重复提交：

- 运行环境无法确认；
- 账号不匹配；
- 委托状态不确定；
- Redis、QMT 或状态库不同步；
- 行情过期或来源冲突；
- 持仓归属不一致；
- 成交无法归属；
- 恢复过程未完成；
- 正式盘未获下单批准；
- 配置缺失或安全开关状态不明。

不得以“保持运行”为理由猜测状态并继续交易。

## 4. 关联项目及角色

- `C:\BigQMT\work\xtquant_big_convert`：大 QMT RPC、Redis、MiniQMT 兼容和启动管理的主要参考。
- `C:\BigQMT\work\Redis`：Windows Redis 运行包来源。
- `C:\BigQMT\work\EasyXT`：QMT 封装与使用方式的辅助参考，不作为权威依赖。
- `C:\BigQMT\work\QMT\QMT-master`：旧 HTTP 桥接参考，不作为正式架构。
- `C:\BigQMT\work\框架示例`：模块边界和数据流参考，不照搬状态存储。
- `C:\BigQMT\research\kitling_AI量化_review`：数据质量、PIT、研究评级和验收证据来源，不作为在线交易进程。
- `C:\BigQMT\research\Alphaforge2`：A 股策略研究域，简称 AF2。
- `C:\BigQMT\research\kitling_AI量化`：ETF 策略研究域，简称 KQ。
- `C:\BigQMT\research\PTtrade策略编辑器\ptrade策略项目\S10-D1_低频动量轮动_RC1-RC2`：首期 v1.1.15 策略来源和对照基线。

参考项目提供证据和设计输入，不能自动获得修改本项目、连接账户或下单的权限。

## 5. 总体架构原则

系统必须分离以下职责：

1. QMT Embedded Bridge：运行于大 QMT 内，封装行情、账户、持仓、委托、成交、下单、撤单和回调。
2. Redis Bus：负责实时消息、请求队列、事件流、最新快照和心跳。
3. Host Gateway：运行于 F 盘主机环境，负责标准化、请求管理、健康检查和状态协调。
4. Execution Engine：负责订单规划、风险检查、幂等、状态机和恢复。
5. Accounting/Portfolio：负责策略资金、持仓归属、成交账、净值和对账。
6. Strategy Runtime：负责纯策略信号和目标仓位，不直接调用券商接口。
7. Monitoring/Dashboard：默认只读，负责状态、进度、收益、异常和证据展示。
8. Data Lake Publisher：后续负责从实时链路发布到 Parquet、DuckDB 和项目 QuestDB，不属于首期前置依赖。

任何策略、网页、研究脚本和数据湖任务都不得绕过 Execution Engine 直接下单。

### 5.1 生产运行所有权

- 部署到 QMT 电脑后，`BigQMT Tray` 是唯一生产运行入口，负责本机服务启动、健康检查、
  策略流程编排、Dashboard 和订单锁。
- QMT 内置 Python Bridge 是唯一券商执行适配层；所有订单必须经过其环境、账户、行情、
  幂等和风险二次校验。
- Codex 仅用于开发、离线验证、审计、报告和人工辅助，不得作为生产策略、订单意图、
  心跳、调度、重试或故障恢复依赖。
- QMT 已登录且 BigQMT Tray 健康时，整个策略流程必须能够在没有 Codex 的情况下运行。

### 5.2 多主机同账户运行与唯一执行权

- 已由用户实测确认：同一证券账户可以在两台 QMT 电脑同时登录；登录能力不等于两台电脑
  可以同时执行策略或下单。
- 已确认部署顺序：开发期间由本机 `192.0.2.105` 继续作为开发和长期运行的主力 QMT，并作为唯一
  模拟 `ACTIVE_EXECUTOR` 候选；开发稳定后才人工迁移到 `192.0.2.125`，`198.51.100.113` 作为后续
  `STANDBY_READONLY`。记录主机角色不能绕过模拟/正式订单门禁。
- 同一账户允许两套 QMT、Tray 与 Bridge 同时在线，但任一时刻只能有一台机器处于
  `ACTIVE_EXECUTOR`；其他机器必须为 `STANDBY_READONLY`。
- 唯一执行权必须按账户发放短时租约并带单调递增 fencing token。未持有有效租约、令牌过期、
  Coordinator 不可达或机器/账户/环境不匹配时，Tray、Host Gateway 与 Bridge 均须拒绝新订单。
- 初期主备接管必须人工确认，并先完成账户、持仓、活动委托、成交与策略账本对账。不得因主机
  短暂断线自动切换并立即下单。
- NAS 只保存发布物、状态副本、备份与审计，SMB 文件或锁文件不得作为账户执行权仲裁器。
- Coordinator 的最终部署位置尚未确定。候选顺序为独立低功耗 Ubuntu 主机、ESXi Ubuntu VM、
  国内云 VPS、Cloudflare Workers + Durable Objects；TrueNAS/OpenWrt/WSL 只作为有条件候选。
- Tailscale、Cloudflare Tunnel 或其他组网产品只提供网络与身份边界，不能单独代替 Coordinator。
- Coordinator 不保存 QMT/券商密码，不直接下单；失联时以停止新增订单换取安全，禁止双主执行机。

## 6. 数据源优先级

### 6.1 当前运行关系

- MiniQMT：在关闭前继续作为现有主系统和对比基线。
- 大 QMT：本项目账户、持仓、委托、成交和行情模块的测试对象；验证期间是影子/候选系统。
- Tushare 及其他第三方来源：行情和历史数据的第二替代及交叉校验来源，不是交易柜台。
- `kitling_AI量化_review` 数据湖：历史、PIT、研究和质量补充，不直接提供券商交易事实。

### 6.2 权威事实

- 券商账户、总持仓、可卖数量、委托和成交以对应环境的大 QMT/券商查询为准。
- 策略自有现金和持仓以可归属真实成交驱动的内部账本为准，并与券商事实对账。
- 行情数据必须保留来源、环境、事件时间、接收时间和质量标记。
- Tushare 不得替代账户、持仓、委托、成交和可卖数量。
- 不得静默混合多个来源生成同一策略信号；发生回退时必须记录原因和数据血缘。

## 7. 存储职责

存储技术不是数据源。每类信息只能有一个权威载体：

- Redis：实时通信、Streams、Pub/Sub、最新快照和心跳；不是永久交易账本。
- SQLite WAL：活动订单、命令幂等、成交去重、恢复、策略资金、持仓归属和对账状态的权威运行库。
- JSON/YAML：配置、注册信息、人工可读快照、进度和发布清单。
- JSONL：追加式命令、回调、成交、错误和状态变更审计。
- Parquet：后续历史行情和数据湖不可变事实的权威载体。
- DuckDB：后续批量扫描、清洗、对账、PIT 查询和不可变 release 视图。
- QuestDB：后续近期热时间序列和项目查询副本；必须可由 Parquet/DuckDB release 重建。

JSON 快照只供查看，不得反向覆盖 SQLite。QuestDB 不得反向修改历史 Parquet。AF2/KQ 不得并发写同一正式 DuckDB release。

首期只要求 Redis、SQLite WAL、JSON/JSONL；Parquet、DuckDB、QuestDB 接入后置。

## 8. Redis约束

- 模拟和正式必须使用不同命名空间；正式只读阶段不得存在可执行订单的消费路径。
- 关键委托、成交和命令使用 Redis Streams 或具有等价确认、重放能力的机制，不能只用 Pub/Sub。
- 每个请求必须有全局唯一 `request_id`。
- 每个事件必须带 `schema_version`、`event_id`、`environment`、`source`、`event_time`、`received_time` 和必要的账号脱敏标识。
- Redis 重复投递不得导致重复订单、重复成交记账或重复资金变化。
- Redis 丢失后，交易状态必须能从 SQLite 和 QMT 券商查询恢复。
- Redis 数据与持久状态冲突时，停止新增风险并进行对账，不能以 Redis 快照覆盖权威账本。

## 9. 订单、成交与恢复不变量

必须把以下对象分开建模：

- 策略信号；
- 目标仓位；
- 待执行意图；
- 券商订单；
- 活动订单状态；
- 真实成交；
- 券商账户持仓；
- 策略归属持仓；
- 策略现金和净值。

强制规则：

- 下单函数返回、Order 回调、资金冻结或持仓快照变化都不等于真实成交。
- 只有可归属、可验证且去重后的真实 Trade/Deal 成交可以修改策略现金和策略持仓。
- 活动订单或委托状态不确定时不得重复提交。
- 每个抑制后续信号或订单的 pending/blocked 状态都必须有可测试退出路径。
- 必须覆盖全成、部成、部成后撤单、拒单、空返回、查询失败、T+1、尾差、重启和回调乱序。
- 重启恢复必须先同步订单、成交、账户持仓和内部状态，再决定是否继续执行。
- 永久不可融资的买入尾差不得无限锁住策略。
- 所有重试必须有限速、可观测、可停止。

## 10. 多策略资金与持仓归属

每个策略使用独立资金袖套，分别记录：

- 初始资金；
- 当前现金；
- 冻结资金；
- 策略持仓；
- 成交和费用；
- 已实现与未实现收益；
- 净值、收益率和最大回撤；
- 资金流入和流出。

策略净值定义为：

`策略现金 + 冻结资金 + Σ(策略归属数量 × 最新估值价格)`

同一账户的持仓必须满足：

`外部基线持仓 + Σ(各策略归属持仓) = 券商账户总持仓`

不得把账户中既有持仓自动认领为某个策略持仓。出现无法解释的持仓变化、负现金、负数量或成交缺失时，阻止新增买入并进入对账状态。

首期策略 v1.1.15 只能使用已授权的 100,000 元资金袖套，利润仅在该袖套内复利，不得使用账户其他资金作为预算或收益分母。

## 11. v1.1.15策略冻结规则

首期迁移以已确认的 v1.1.15 为父版本和对照基线。工程迁移不得顺便改变信号逻辑。

冻结范围包括但不限于：

- U25 ETF 池及备用标的；
- 25 日 legacy weighted-slope 动量；
- 评分范围、SMA4 和 5% 换仓改善阈值；
- RC1 最短持有 5 日；
- 日跌幅、回撤和止盈规则；
- 止损/止盈冻结期；
- 单持仓、98% 策略资金、100 份整数手；
- 收盘后信号、下一交易日执行；
- 交易窗口、零量限制和买卖价格保护。

策略信号、QMT 适配、执行状态机和资金核算必须分别验证。任何实质行为改变必须创建新版本，不覆盖 v1.1.15 及其历史证据。

## 12. AF2与KQ接入规则

- AF2 是 A 股策略研究域，KQ 是 ETF 策略研究域。
- 两者共享大 QMT Gateway、Redis、执行、核算、监控和数据合同，但保留独立策略、证券池、风险规则和研究状态。
- 当前 review 评级为 `RESEARCH_ONLY` 的策略只能进入影子运行，不能直接获得模拟或正式下单资格。
- ETF 与 A 股不能共用同一套停牌、涨跌停、T+0/T+1、费用、容量和资产池规则。
- 首期先完成 v1.1.15 ETF 闭环，再评估 KQ ETF 候选，最后评估 AF2 A 股候选。
- 初期不进行策略间订单净额合并，优先保证成交归属和审计清晰。

## 13. 数据湖后置原则

大 QMT 基础链路、模拟订单和恢复对账通过前，不把数据湖接入作为前置依赖。

后续目标数据流：

`大QMT → Redis → F盘落盘缓冲 → Bronze Parquet → DuckDB质量处理 → Silver/Gold Parquet → immutable DuckDB release → AF2/KQ QuestDB热副本`

要求：

- Bronze 原始数据只追加，不覆盖。
- 研究价格与真实执行价格分离。
- 所有历史证券池保持 PIT，不能使用当前股票/ETF 列表回填历史。
- release 发布后不可变，使用新版本替代覆盖。
- QuestDB 只保存热数据和可重建副本。
- 大 QMT 项目首期不新增第三个 QuestDB 实例。
- 数据质量门禁失败时不更新 `LATEST`，不切换 QuestDB 热表。

## 14. 测试与阶段门禁

项目按以下顺序推进，不得跳级：

1. `P00_PLANNING`：规划、目录、合同和安全边界。
2. `P01_REDIS`：Redis 通信、Streams、心跳和恢复。
3. `P02_QMT_BRIDGE`：大 QMT Bridge 启动和版本验证。
4. `P03_READ_ONLY`：模拟盘账户、持仓、委托、成交只读。
5. `P04_MARKET_DATA`：大 QMT 实时行情、延迟、缺口和重连。
6. `P05_RUNTIME_STATE`：SQLite、JSON、幂等和恢复。
7. `P06_SIMULATION_ORDERS`：受控模拟下单、撤单和异常路径。
8. `P07_RECONCILIATION`：委托、成交、持仓、现金和状态机对账。
9. `P08_V1_1_15`：信号一致性、独立资金、模拟执行和收益重放。
10. `P09_MULTI_STRATEGY`：KQ/AF2 影子策略和多资金袖套。
11. `P10_DASHBOARD_DATA_CONTRACT`：只读状态投影、数据契约和单页基础展示。
12. `P11_DATA_LAKE`：Parquet/DuckDB/QuestDB 后续接入。
13. `P12_SIMULATION_ACCEPTANCE`：连续稳定性、恢复和完整模拟验收。
14. `P13_PRODUCTION_READ_ONLY`：正式盘只读验证。
15. `P14_TRAY_MANAGER_CORE`：托盘运行管家、双 profile、日志和订单锁。
16. `P15_TRAY_RUNTIME_HEALTH`：QMT/Redis/Bridge/Dashboard 健康检查与安全恢复。
17. `P16_AUTO_START_LOGIN`：最后测试自动启动与自动登录。
18. `P17_PRODUCTION_CANDIDATE`：仅在用户另行批准后讨论正式下单。
19. `P18_DASHBOARD_PRODUCT_RND`：六个只读路由页面、策略收益展示与页面级验收。
20. `P19_PORTABLE_DEPLOYMENT_UPGRADE`：跨 QMT 电脑部署、升级与回滚。
21. `P20_OPERATIONS_BACKUP_ALERTING`：备份、恢复演练、告警与运行手册。
22. `P21_REMOTE_READONLY_OBSERVABILITY`：经明确授权的局域网只读观测。
23. `P22_STRATEGY_ONBOARDING_GOVERNANCE`：A 股/ETF 策略准入、资金袖套和版本治理。
24. `P23_DATA_LAKE_SCHEDULE_RETENTION`：数据湖调度、校验和留存。
25. `P24_COORDINATOR_FOUNDATION`：节点注册、租约、fencing、epoch、订单意图和可迁移 Ubuntu 服务。
26. `P25_WINDOWS_FLEET_HOST_AGENT`：双 Windows QMT 主机心跳、只读快照、意图拉取与本机最终校验。
27. `P26_OPENCLAW_MCP_SKILL`：中央 MCP、OpenClaw Skill、Agent 分权和手机只读查询。
28. `P27_REMOTE_SIMULATION_EXECUTION`：手机预览/确认、唯一执行机领取、模拟成交与策略账本闭环。
29. `P28_MULTI_HOST_MIGRATION_ACCEPTANCE`：主备人工接管、Coordinator 迁移、防双主、发布和灾难恢复验收。

模拟盘订单测试前必须完成只读、行情、状态和安全开关验证。正式盘订单测试前必须完成整个模拟验收，而不是仅完成单次买卖。

## 15. 模拟盘验收条件

只有同时满足以下条件，才能把状态标为 `SIMULATION_VERIFIED`：

- 账户、持仓、委托、成交字段已在目标模拟终端验证；
- Redis 实时行情和关键事件传递稳定；
- 下单默认关闭且开启过程可审计；
- 全成、部成、撤单、拒单、超时和状态不确定路径通过；
- 重复请求不会产生重复订单；
- 重启后可以恢复活动订单和账本；
- 券商成交、内部成交、策略现金、策略持仓和账户持仓能够对账；
- v1.1.15 信号规则与基准一致；
- 100,000 元策略袖套复利核算可以从成交重放；
- 连续不少于 20 个交易日的影子/模拟稳定性证据完成；
- 没有未解释的永久锁单、超支、持仓归属差异或错误风暴；
- 已生成模拟验收报告、配置哈希、代码版本和回滚版本；
- 用户明确确认模拟盘验收完成。

测试通过一次只能标为 `PASSED`；阶段证据完整并经确认后才能标为 `VERIFIED`。

## 16. 正式盘只读规则

正式盘只读验证可以在模拟验收前进行，但必须满足：

- 使用专门的 `production_readonly` 配置；
- `orders_enabled=false` 为硬编码默认和运行时校验双保险；
- RPC 服务端订单白名单关闭；
- 客户端不暴露下单和撤单命令；
- Dashboard不显示正式盘交易按钮；
- 通用 RPC 不得调用订单方法；
- 每次连接显示醒目的 `PRODUCTION READ ONLY` 状态；
- 只读查询保留审计记录，但不记录明文账号和密码；
- 发现环境识别失败立即断开或降级，不尝试猜测环境。

正式盘只读验证不能改变 `SIMULATION_VERIFIED` 状态，也不能作为跳过模拟盘测试的理由。

## 17. 自动启动与自动登录规则

自动启动、重启和登录放在最后阶段。其目标是运维自动化，不是交易功能本身。

- 优先验证 QMT 自身会话恢复和显式 `mode=exe`。
- 禁止使用会因发现 `XtMiniQmt.exe` 而启动 MiniQMT 的不明确 `mode=auto`。
- 仅在必要时测试窗口自动输入的 `mode=login`。
- 登录密码不得写入 Git、JSON、YAML、批处理、脚本参数或日志。
- 自动登录不得在锁屏、RDP 注销或需要验证码/二次确认时强行操作。
- 58600 端口监听不等于登录和桥接成功。
- 真正就绪必须同时验证主窗口、Redis、RPC ping、账户、持仓和行情。
- 自动启动失败必须保持订单关闭，不得循环重启并盲目提交。

## 18. 进度、证据与状态查询

进度文件必须保持人类可读：

- `progress/project_plan.yaml`：阶段、验收项和权重。
- `progress/current_status.json`：当前阶段、环境、开关和下一门禁。
- `progress/latest_tests.json`：最近测试结果和证据路径。
- `progress/blockers.json`：阻塞原因和解除条件。
- `progress/history/`：不可覆盖的历史进度快照。

测试状态只使用：

`NOT_STARTED / IN_PROGRESS / PASSED / FAILED / BLOCKED / VERIFIED / DEFERRED`

进度百分比必须由已验证验收项计算，不得凭主观填写。任何 `VERIFIED` 状态都必须包含：

- 测试时间；
- 目标环境；
- 代码版本；
- 配置哈希；
- 测试报告；
- 关键日志或导出证据；
- 未解决限制。

## 19. 配置、密钥与日志

- 账号、Redis 密码、QMT 密码和其他凭据不得提交到版本库。
- 日志必须脱敏账号、密码、Token 和个人信息。
- 模拟与正式配置文件必须物理分离，不能依靠一个容易误改的布尔值切换环境。
- 正式配置默认只读，订单权限必须由多重检查明确开启。
- 日志和审计记录按环境、日期、模块分开保存。
- 不使用日志文本推断真实成交；成交必须回到券商查询和可归属成交事实。
- `config/machine.local.json` 是目标机唯一机器覆盖文件。P19 迁移改造后的目标契约允许在一个文件
  中配置 BigQMT 根目录、模拟/正式 QMT 根目录、运行数据、状态库、审计、日志、备份、Redis、
  staging、数据湖目录及 Redis/Dashboard/FormulaServer/Coordinator 端口或地址；可推导的子目录优先
  使用相对路径。本文件不含账户密码、服务密钥、策略准入或正式下单授权，也不能翻转订单权限。
  当前实现仍只覆盖原三类变量且存在旧绝对路径回退，必须在 P19 M1 完成严格 schema、全入口收口和
  损坏配置失败关闭后，才能宣称达到该目标契约。

## 20. 版本与变更管理

- 任何已测试、已回测、已部署的策略或桥接版本不得原地覆盖。
- 实质行为变化必须新建版本并记录父版本、差异、验证和回滚版本。
- 同步文件到 QMT 后必须验证实际加载版本，不能只凭复制成功判断生效。
- 模拟和正式部署包分别生成 manifest 和 checksum。
- 未验证的版本标为 `CANDIDATE`，不得伪装成稳定版。
- 修复执行或核算问题时不得顺便修改信号；确需同时修改时必须拆分版本和验证证据。

## 21. 完成定义

任务只有在以下条件满足时才算完成：

- 请求范围内的实现、文档和配置完整；
- 相关自动测试通过；
- 目标 QMT 终端证据已经收集，或明确标记仍待终端验证；
- 安全开关处于预期状态；
- 进度文件已更新；
- 测试报告和回滚方式可定位；
- 没有用模拟证据冒充正式证据；
- 没有用只读连接暗示获得正式下单许可。

本项目的首要价值不是“能下单”，而是每一条行情、信号、订单、成交、持仓和收益都能说明来源、状态、归属和验证证据，并且在任何不确定情况下安全停止新增风险。
