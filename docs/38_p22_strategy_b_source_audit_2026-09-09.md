# P22 策略 B 来源盘点与入池门禁（2026-09-09）

## 盘点范围

只读检查 `C:\BigQMT\research\kitling_AI量化_review` 的项目记忆、进度文件和脚本目录，没有复制、修改或运行该项目代码。

## 当前可确认事实

- `PROJECT_PROGRESS.yaml` 将 KQ 项目标记为 ETF 研究项目，ETF 动态资产池已经有代码证据。
- ETF 价格复权/交易日历仍为 `DOING`，统一 ETF 策略接口、多策略隔离、独立 WFA、统一成交账本等关键项仍为 `TODO`。
- 进度文件记录的 v22e/v23/v24 结果仍是研究口径；v24 曾记录净值记账 bug 待重跑，不能直接作为 QMT 执行契约。
- 当前没有发现可直接交付给 QMT 的、带固定版本号、固定标的池、完成 bar 频率、信号发布时间、交易成本和 T+1 规则的“策略 B 发布包”。

## 入池结论

策略 B 保持 `SHADOW_ONLY`，不生成 QMT 下单信号，不接入模拟账户执行袖套。现有 `config/strategy_registry.json` 的策略 B 登记只表示候选身份和资金上限，不表示已通过执行审批。

## 晋级所需材料

1. 固定 release/version 与不可变文件清单。
2. 固定 QMT 证券代码及生效日期，完成 `.SZ/.SH` 规范化。
3. 固定信号契约：数据频率、收盘/盘中时点、复权口径、换仓阈值、最小持有期、手数和 T+1。
4. 完成独立 WFA、成本/滑点敏感性和至少连续 10 个交易日影子运行。
5. 生成一份与策略 A 相同格式的 sleeve/NAV/审计证据，再由用户单独批准进入模拟执行。

## 下一步

优先做策略 B 发布包字段审计和标的池确认；未满足上述材料前，不修改 `execution_enabled=false`，也不扩大模拟账户允许策略范围。
