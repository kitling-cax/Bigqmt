# v1.1.15 直接模拟执行授权记录

记录时间：2026-09-11。

用户明确调整了原先的 20 个交易日影子观察安排：**不以 20 日观察作为 v1.1.15 模拟账户下单门槛**。首期策略可在模拟账户 `90000001` 直接执行。

## 仍然不变的边界

- 仅策略 `S10_D1_U25_NO_ALCOHOL_5D_SIM_MAIN_V1_1_15` 可进入模拟自动执行；策略 B 继续 `SHADOW_ONLY`。
- 仅使用独立 100,000 元资金袖套，目标资金使用率 98%，100 份整数手，收益仅在袖套内复利。
- 启动模式是 `ISOLATE_NEW`：历史影子成交不是实盘成交，账户既有持仓不是策略持仓；不得自动卖出或认领。
- 每次候选订单仍必须经过实时账户、未完成委托、最新行情、限价保护、袖套预算和账户身份核验。
- QMT 运行时权限是短时窗口；收到一次提交结果或任何超时/异常立即回锁，不进行盲目重试。
- 正式账户 `90000002` 维持独立物理配置、RPC 订单白名单关闭、运行时只读；本授权不改变正式盘。

## 与旧记录的关系

`docs/51_next_execution_sequence_record_2026-09-10.md` 的“20 日影子后再下单”和“逐单人工确认”两项被本文件替代。影子证据继续用于信号连续性、行情质量和状态恢复审计，但不再是模拟订单许可本身。

首次策略订单只取**本授权日以后**产生的一个完整收盘信号，并在随后有效交易时段执行；不会追补此前的虚拟信号或虚拟持仓。
