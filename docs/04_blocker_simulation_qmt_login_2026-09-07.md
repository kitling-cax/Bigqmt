# 阻塞记录：模拟版 QMT 无法登录

日期：2026-09-07

## 现象

- 模拟版 QMT 当前无法登录。
- 具体错误提示、登录阶段和网络/券商返回信息尚未收集。

## 影响范围

- P02 大 QMT Bridge 无法在 QMT 模型交易环境中完成启动验证。
- 暂不能验证账户、持仓、委托、成交接口。
- 暂不能验证 QMT 实时行情经 Redis 传递到项目。
- 暂不能进行模拟下单验证。
- 正式版仍保持只读，不受本阻塞影响，也不允许切换为下单环境。

## 已保留的可用状态

- QMT 已位于 F 盘新路径。
- 模拟 Redis 仍配置在 `127.0.0.1:6379`，数据落盘到 F 盘。
- Bridge 文件已复制到模拟版 QMT 的 `python` 目录，但尚未在 QMT 界面加载。
- 未复制首期 `v1.1.15` 策略。
- 未执行任何下单或撤单。

## 后续处理原则

1. 先收集模拟版 QMT 的登录错误信息和相关日志。
2. 只处理模拟环境，不改正式版登录状态或交易配置。
3. 登录恢复后，先做 QMT 只读连通性验证，再继续 Bridge 验证。
4. 未完成模拟盘验证前，不进入正式盘交易和自动登录开发。

