# Dashboard 白底主题与错误信息整理（2026-09-09）

## 变更

- Dashboard 改为白底、浅灰工作区、白色卡片和轻边框，保留蓝色主色与黄色安全提示；
- 英文状态码转换为中文，例如“模拟盘”“运行中 · 非交易时段”“已锁定”；
- 非交易时段不再显示为错误，而是显示为“等待下一个交易时段刷新”；
- 增加简洁的“验证与安全状态”区，明确正式账户只读、模拟账户等待预检；
- 保留策略袖套、NAV、收益率和 NAV 点数；
- 手机宽度 390px 下自动单列，无横向页面溢出；
- Dashboard 仍只有只读 API，没有下单、撤单或确认接口。

## 验证

Playwright 检查结果：

- 桌面 1440px：白底、策略袖套可见、无控制台错误、无横向溢出；
- 手机 390px：单列布局、无横向溢出、无控制台错误；
- `/healthz` 与 `/api/status` 正常；
- 当前模拟账户显示 5 个外部基线持仓、2 个策略袖套；
- 订单状态仍为“已锁定”。
