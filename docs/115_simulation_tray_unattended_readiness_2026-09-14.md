# 模拟盘托盘无人值守就绪检查

**时间**：2026-09-14 08:24（Asia/Shanghai）  
**配置**：模拟账户 `90000001` / Redis `6379` / Dashboard `17890`

## 结果

- BigQMT Tray 已通过修正后的脱离启动器启动，当前进程 PID `12952`；消息循环已写入 `tray_message_loop_starting`，并持续驻留。
- Redis PING 通过；Bridge 只读 ping 通过；Dashboard `/healthz` 通过。
- 只读快照刷新成功：总资产 `10,075,933.61` 元，6 个持仓，0 委托，0 成交，6 个行情快照；快照 run_id 为 `e4933c08d5e64776a288314c36e29fbc`。
- 托盘健康总评为 `HEALTHY`；订单锁当前为 `READ_ONLY_LOCKED`。

## 无人值守执行边界

模拟策略主开关 `v1_1_15_auto_run_enabled=true`。工作日 09:35–14:50，托盘每天只调度一次 v1.1.15 周期；Python 周期会重新检查账户、行情、未完成委托、袖套归属、交易日和 QMT Bridge 执行准入。只有单个、可归属的模拟策略动作通过检查时，才建立最长 120 秒的临时执行窗口；响应或超时后立即重新加锁。

QMT 进程缺失时托盘可以请求启动；Redis 缺失时只启动对应端口实例。对异常终端不会自动杀进程或循环重启，也不会把正式盘纳入执行。正式账户 `90000002` 仍为只读。

## 重要说明

托盘健康并不保证今天一定有交易：没有新收盘信号、持有期未满足、风控阻断、行情/账户不一致或 Bridge 执行能力未加载时，系统会记录 `BLOCKED`/`ALIGNED_NO_ORDER` 并保持锁定。这是预期的安全行为。
