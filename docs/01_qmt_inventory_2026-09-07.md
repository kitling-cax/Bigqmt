# QMT 迁移前只读盘点

日期：2026-09-07（路径更新）

## 当前状态

- 本次只读盘点，没有复制、移动、删除或修改 QMT 文件。
- 模拟版和正式版 QMT 相关进程均已从 F 盘新路径运行。
- 用户已将模拟版与正式版目录迁移到 F 盘，程序入口分别为：
  - `C:\BigQMT\work\国金QMT交易端模拟\bin.x64\XtMiniQmt.exe`
  - `C:\BigQMT\work\国金证券QMT交易端\bin.x64\XtMiniQmt.exe`
- 两套终端均发现 `XtItClient.exe` 与 `miniquote.exe` 进程。

## 磁盘空间

| 盘符 | 容量 | 可用空间 |
|---|---:|---:|
| E: | 约 59.7 GB | 约 49.9 GB |
| F: | 约 5.59 TB | 约 4.82 TB |

原先的 E 盘路径已不再作为运行路径；F 盘现在同时承载 QMT 程序、QMT 原生数据和项目长期数据。

## 已发现的 QMT 数据目录

两套 QMT 结构基本一致，包含：

- `data`
- `datadir`
- `userdata`
- `userdata_mini`
- `config`
- `python`
- `bin.x64`

模拟版 `datadir` 已包含 `SH`、`SZ`、`Industry`、`Sector` 等行情和板块数据；`userdata` 中存在客户端状态、日志、用户配置和交易数据目录。因此迁移时不能只复制 `bin.x64`，需要将整个终端目录作为候选迁移单元，并保留原目录回滚。

## 迁移决策

1. 以 F 盘新路径作为当前唯一运行路径。
2. 先验证模拟版的启动、行情、账户、持仓、委托和成交查询。
3. 模拟版验证通过后，正式版仅以只读方式接入，禁止下单。
4. E 盘旧目录如仍存在，仅作为回滚备份，不再作为运行路径。

## 下一步门槛

迁移动作已经由用户完成；下一步只做 F 盘新路径下的启动、行情、账户、持仓、委托和成交只读验证。任何模拟下单前仍需先完成桥接、Redis、状态库和恢复测试。
