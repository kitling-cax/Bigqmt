# P02 双账户 Bridge 只读连通性验证（2026-09-10）

## 结果

在用户确认模拟和正式 QMT 均运行 `BIGQMT_BRIDGE` 后，项目重新启动了本机隔离 Redis：模拟 `6379`、正式 `6380`。两套只读快照均成功。

| 环境 | 账户 | 快照 run_id | 持仓 | 委托 | 成交 | 行情 |
|---|---|---|---:|---:|---:|---:|
| simulation | `90000001` | `7949c3f26a444915b945e210da0564ee` | 6 | 0 | 0 | 6 |
| production_readonly | `90000002` | `ff2d94adf1434b76a30808b02c549cc4` | 1 | 0 | 0 | 1 |

## 安全边界

- 本次只调用账户、持仓、委托、成交和行情读取接口；没有调用下单、撤单或执行消费者。
- 两套托盘的订单锁仍为 `READ_ONLY_LOCKED`。
- 正式账户数据只写入独立的正式状态库，不进入模拟 Dashboard；正式 Dashboard 投影仍待 P13。
- 模拟账户已有的 510300 T+1 状态未被修改，也没有新增委托。

## 下一步

收盘后由模拟托盘自动执行一次数据湖周期；然后检查导出证据与 Dashboard 告警。正式账户继续只读，不开启正式数据湖自动周期。
