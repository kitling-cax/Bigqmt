# 后续执行顺序确认记录

记录时间：2026-09-10。此记录不改变 QMT、Redis、Bridge、托盘或订单权限。

## 当前固定边界

- 模拟账户：`90000001`；订单能力当前锁定。未来每一笔策略真实模拟订单仍须经过预检、Dry-run、袖套预算和用户逐单确认。
- 正式账户：`90000002`；持续只读。没有策略执行、下单或撤单计划。
- 账户原有持仓属于外部基线，不自动归属策略，不参与策略再平衡或自动卖出。

## 已确认执行顺序

1. P12：托盘每日 15:35 后自动运行 v1.1.15 收盘影子信号，累计至少 20 个交易日证据。
2. P12：每日核对 QMT Bridge 快照、Redis、行情完整性、状态恢复与外部基线隔离。
3. 固化 v1.1.15 模拟策略参数、袖套边界、证券映射、T+1 和风险约束，生成只读待执行计划。
4. 影子期通过后，先完成模拟盘预检、预算校验和 Dry-run；不自动下单。
5. 仅在用户明确确认某次证券、方向、数量和价格范围后，允许模拟盘 Bridge 执行对应首单。
6. 随已归属的模拟真实成交推进 P18.3 策略袖套 NAV、收益、回撤和曲线。
7. 推进 P20 备份日常调度与告警、P19 新主机迁移演练。
8. P21 局域网远程只读查看需要单独网络与认证授权；P16 自动登录最后独立处理。

## 当前下一门禁

持续完成 v1.1.15 的 20 个交易日收盘影子验证。当前前复权 QMT 数据口径仍标记为与 PTrade 未验证等价，因此不能据此解锁任何策略订单。
