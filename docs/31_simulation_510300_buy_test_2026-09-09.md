# 模拟 Bridge：510300.SH 首笔柜台买入验证（2026-09-09）

状态：`BUY_CONFIRMED_T1_SELL_PENDING`。

## 授权范围

用户明确授权模拟账户 `90000001` 对华泰柏瑞沪深 300 ETF（`510300.SH`）买入 100 股后卖出。本次不涉及正式账户 `90000002`，不属于 v1.1.15 策略委托。

## 预检与受控放行

- 模拟 Redis `127.0.0.1:6379`、BIGQMT_BRIDGE ping、账户、持仓、委托、成交与行情均已恢复；
- 买前账户无 `510300.SH` 持仓、无委托、无成交；
- QMT 策略运行模式修正为实盘执行模式，账户仍为模拟账户；
- 仅模拟本地配置短时开放 RPC 订单方法，静态策略白名单仅允许 `SIMULATION_510300_BRIDGE_TEST_20260909`；
- 运行时许可绑定账户、标的、100 股测试意图并设过期时间；正式环境文件未修改。

## 买入事实

| 项目 | 结果 |
|---|---|
| 委托 | `200` |
| 成交 | `50055942` |
| 方向 | 买入 |
| 数量 | 100 股 |
| 限价 / 成交价 | 4.640 / 4.640 |
| 手续费 | 0.1392 |
| 委托标签 | `sim-510300-buy-20260909144603` |
| 归属策略 | `SIMULATION_510300_BRIDGE_TEST_20260909` |

下单证据：`runtime_data/evidence/simulation/one_time_510300_buy_20260909_144604.json`。

## 未卖出的原因

成交后的 QMT 事实快照为 `volume=100`、`available=0`、`on_road_volume=100`。该证券当日不可卖，故未提交卖单，也没有撤单或重复买入。下一交易日需重新确认可卖数量、实时价格和用户卖出授权后才能执行 100 股卖出测试。

## 回锁与恢复

- `runtime_data/control/simulation/runtime_control.json` 已恢复 `READ_ONLY_LOCKED`；
- 模拟本地配置磁盘文件已恢复 `rpc_allow_order_methods=false` 与空白执行白名单；
- 当前运行 Bridge 在下一次停止/启动前可能仍保有已加载的静态配置，但动态订单门禁已即时锁定并失效，无法授权新增订单；
- 正式账户配置和 Redis `6380` 未改动。
