# v1.1.15 模拟账户首笔策略执行

执行时间：2026-09-11 10:20（Asia/Shanghai）  
账户：`90000001`（模拟）  
策略：`S10_D1_U25_NO_ALCOHOL_5D_SIM_MAIN_V1_1_15`

## 预检

- `BIGQMT_BRIDGE` 已重启，Ping 返回 `allow_order_methods=true`、账号 `90000001`。
- 最新完整收盘信号日为 `20260910`，目标 `160723.SZ`；本次在下一交易日执行。
- 下单前 `160723.SZ` 不在券商持仓中，账户无未完成委托；不会认领或交易既有持仓。
- 10:20 买一价约 `2.359`，按 0.3% 保护限价为 `2.366`。
- 独立袖套初始资金 100,000 元；下单 `41,400` 股，预估费用后仍在 98% 目标资金使用率内。

## 结果

| 项目 | 结果 |
| --- | --- |
| 委托号 | `365` |
| 委托状态 | `56`（全成） |
| 成交 | `1010000032383094`、`1010000032383095` |
| 成交数量 | `41,400` 股 |
| 实际成交均价 | 约 `2.36017` |
| 实际成交费用 | `29.31336` 元 |
| 委托标签 | `v1_1_15-entry-20260910-14dac123f8` |

策略袖套成交后现金为 `2,259.48664` 元。以 10:20 后 QMT 最新价 `2.359` 估值，首个净值为 `99,922.08664` 元；这是开仓当刻的市价与手续费结果，不是策略绩效结论。

## 对账与锁定

券商总持仓 = 外部基线持仓 + 策略归属持仓，差异为 0。`160723.SZ` 的 41,400 股已归属该策略；`511010.SH`、`511990.SH`、`513100.SH`、`513500.SH`、`518880.SH` 保持外部基线属性。

QMT 提交响应后，`runtime_control.json` 已立即恢复 `READ_ONLY_LOCKED`。本首单不意味着开放账户通用交易权限；下一笔 v1.1.15 订单必须先完成成交/持仓/资金重对账和新的短时预检。正式账户 `90000002` 始终未参与。

原始提交证据：`runtime_data/evidence/simulation/strategy_execution/v1_1_15_entry_20260911_102034.json`。
