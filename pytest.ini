[pytest]
# The production package lives under src/ while a few operational helpers are
# imported from scripts/ by their tests.  Keep both import roots explicit so
# `pytest` and `python -m pytest` behave identically on a clean workstation.
pythonpath =
    src
    .
testpaths =
    tests
