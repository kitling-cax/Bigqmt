# Changelog

All notable source changes are versioned through Git tags and immutable NAS
release manifests.

## Unreleased

- Establish GitHub source-control baseline for `.105`, `.125`, `.113` and the
  Coordinator container.
- Host Agent portable package `0.2.1` corrects the Fact Secret destination to
  protected local ProgramData storage; release binaries remain outside Git.
